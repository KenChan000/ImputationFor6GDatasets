# Core scientific stack
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.12,<2
scikit-learn>=1.5,<2
 
# Notebook tooling
jupyterlab>=4.2,<5
notebook>=7.2,<8
ipykernel>=6.29,<7
 
# Classical imputation
fancyimpute>=0.7,<1                # SoftImpute
 
# Deep tabular imputation
hyperimpute>=0.1.17                # also covers MICE-style workflows
 
# Plotting helpers
matplotlib>=3.9,<4
seaborn>=0.13,<1
 
# Notes:
# - scikit-learn provides mean, median, kNN (KNNImputer), and MICE (IterativeImputer).
# - torch and torch-geometric are NOT pinned here. They need CUDA-matched wheels from
#   PyTorch's index, so the setup script installs them separately.
# - GRAPE and DiffPuter are not on PyPI. They are cloned into external/DiffPuter
#   by the setup script and imported from there.
#
# Run: bash scripts/setup_imputation_env.sh
# Flags: --skip-grape  --skip-diffputer  --update-locks